---
import type { Program as programProps } from '../tv/domain/Program';
import ProgramCard from './ProgramCard.astro';

interface Props {
  program: programProps;
  count?: number;
  key?: string;
}
---

<ProgramCard program={Astro.props.program} count={Astro.props.count} key={Astro.props.key} />
<slot />